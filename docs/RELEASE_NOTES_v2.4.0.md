# PinteMod Control Center v2.4.0

## Nouveautés et fiabilisation

- rendu logiciel WPF disponible pour les écrans blancs en prise en main distante ;
- Agent SMB récupérable et Agent autonome inclus dans le dossier portable ;
- retour RCON Community Pause v0.4 reconnu ;
- vérificateur PinteMod compatible avec l’inventaire actuel de 35 modules ;
- absence de `hotfix.gsc` Ezz BOIII reconnue comme normale ;
- réparation PinteMod existante limitée au vérificateur stock connu : aucun module ni service existant n’est écrasé.

## Livraison

- `PinteMod.ControlCenter-single-exe-win-x64.zip` : EXE Windows x64 autonome ;
- `PinteMod.ControlCenter-folder-win-x64.zip` : dossier portable incluant le lanceur de compatibilité et l’Agent autonome ;
- `SHA256SUMS.txt` : empreintes SHA-256 ;
- `SELF-TEST.txt` : preuve d’auto-diagnostic hors-ligne.

Validation : Debug et Release sans avertissement, 613/613 tests réussis, auto-diagnostic et audits de paquet réussis. Conservez une sauvegarde et validez d’abord sur Server3 avant un déploiement plus large.
